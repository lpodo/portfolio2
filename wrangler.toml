# Portfolio Terminal 2 — TEST worker (experimental branch)
# Used by Cloudflare's "Connect to Git" integration to deploy worker-test.js
# from this branch. The production worker (main branch) is unaffected.

name = "portfolio-worker-test"
main = "worker-test.js"
compatibility_date = "2025-01-01"
